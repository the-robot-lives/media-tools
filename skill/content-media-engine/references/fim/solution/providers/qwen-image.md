# Qwen Image 3.0 — DashScope text-to-image / image-to-image

Use `service: qwen-image` with model `qwen-image-3.0` (default) or `qwen-image-3.0-pro`.

Auth: `DASHSCOPE_API_KEY`, `QWEN_API_KEY`, or `QWEN_TOKEN_KEY`. Token-plan host: `provider_options.plan: token-plan`.
